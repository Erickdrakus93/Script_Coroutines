
// Here we can use this function to the inner of a HTML Script
function my_function(voidFunction) {
    document.getElementById("demo").innerHTML = "paragraph changed";
    // This is the

}
// This is the another form of the use of the main function as we can see in the next lines of the code

function another_function(voidFunction) {
    document.getElementsByName("This is the demo").entries();

}

function this_is_the_function(voidFunction) {
    document.getElementById("This the name of the class").innerText = "This is the name of the class";
}

// Here we declare an array as we can see in the main structure of the context part of the main example
// Here is not in inn structure
let fruits = ["Banana", "Sandia", "Pera"];
document.getElementById('Demostrate').innerHTML= fruits;
// A array of strings; as we can see in the inner functions //
fruits.length; // This returns the len of the array;
